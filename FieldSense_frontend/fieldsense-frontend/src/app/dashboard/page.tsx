export const metadata = {
	title: 'Dashboard | FieldSense',
};

export default function DashboardPage() {
	return (
		<div style={{ padding: '2rem' }}>
			<h1>Dashboard</h1>
			<p>This placeholder dashboard page was auto-generated because the file was empty and caused the build to fail.</p>
		</div>
	);
}
